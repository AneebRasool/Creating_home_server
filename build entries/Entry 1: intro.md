### Entry 1: starting the project
Date: July 20 2026

Ive decided to convert my old laptop into a dedicated server, to be used mainly as a NAS but to also build skills like working with linux and networking, and learn some things about managing my own projects. Im currently planning on using  `SMB` for home, as it would be faster for smaller files, and can natively be mapped as a network drive on windows (as well as on android, I use cx file explorer). Then I can use `tailscale` which encrypts files, so I can securely transfer files from outside of home.

For this project I am going to install `ubuntu linux server`, due to low ram usage for less overhead, but also to learn more practically with `CLI's`.
